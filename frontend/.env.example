
# Chronomark is deployed and these default to the real addresses in
# src/config/chains.ts. Only set these if you redeploy the contract and
# need to point the app at a different address without editing source.

VITE_CONTRACT_ADDRESS_STUDIONET=
VITE_CONTRACT_ADDRESS_BRADBURY=
